package model

// Enum class representing the result of a round in the game
enum class RoundResult {
    PLAYER_ONE_WIN, // Player One wins the round
    PLAYER_TWO_WIN, // Player Two wins the round
    DRAW            // The round ends in a draw
}